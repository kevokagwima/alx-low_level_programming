#include <stdio.h>

/**
 * main - this function uses printf to print out a text
 * return (0) - return te value 0
 */

int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}

